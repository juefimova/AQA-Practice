# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: popup.spec.ts >> Open and Close Modal window
- Location: tests/popup.spec.ts:4:5

# Error details

```
Error: expect(locator).toBeHidden() failed

Locator:  locator('.modal-content')
Expected: hidden
Received: visible
Timeout:  5000ms

Call log:
  - Expect "toBeHidden" with timeout 5000ms
  - waiting for locator('.modal-content')
    14 × locator resolved to <div class="modal-content">…</div>
       - unexpected value "visible"

```

```yaml
- button "×"
- heading "It’s that Easy!! Well I think it is....." [level=4]
- paragraph: We can inject and use JavaScript code if all else fails! Remember always try to use WebDriver Library method(s) first such as WebElement.click(). (The Selenium development team have spent allot of time developing WebDriver functions etc).
- button "Close"
```

# Test source

```ts
  1  | import {test, expect} from "@playwright/test";
  2  | import {PopupPage} from "../pages/PopupPage";
  3  | 
  4  | test('Open and Close Modal window', async({page}) => {
  5  |     const popup = new PopupPage(page);
  6  |     await popup.navigate();
  7  |     await popup.openWindow();
  8  |     await expect(popup.window).toBeVisible();
  9  |     await popup.readMessage();
  10 |     await popup.closeWindow();
> 11 |     await expect(popup.window).toBeHidden();
     |                                ^ Error: expect(locator).toBeHidden() failed
  12 | })
```