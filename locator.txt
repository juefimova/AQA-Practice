   1. CSS селекторы
   1) [data-test-id="email-input"]
   2) #pass
   3) .btn-primary.submit-btn

   2. XPath локаторы
   1) //h2[text()="Вход в систему"]
   2) //*[contains(text(), "Забыли пароль?")]
   3) //span[text()="Войти"]/..

   3. A) локатор /html/body/div[1]/div[2]/input плох, потому что это абсолютный путь, и при изменении структуры
   мы не сможем обратиться к этому элементу
      B) .ext-2913 плох тем, что мы обращаемся к классу в целом, туда могут добавить и другие элементы, и название
      выглядит так, будто класс генерируется, поэтому его имя может измениться


   4. А) #searchInput
         //span[contains(text(), "Случайная статья")]

      B) [data-test="add-to-cart-sauce-labs-backpack"]

   *) //td[text()="Alden"]/parent::tr//span[@title="Edit"]
